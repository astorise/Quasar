{
  "name": "@quasar/sdk-workspace",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "analytics-core",
    "analytics-replay"
  ],
  "scripts": {
    "build": "rollup -c rollup.config.mjs",
    "check": "npm --workspaces run check --if-present"
  },
  "devDependencies": {
    "rollup": "^4.41.0"
  }
}
